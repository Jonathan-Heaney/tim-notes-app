// These are here because we're using Local Storage to store the access tokens in the browser, and you need a key to access them in local storage
export const ACCESS_TOKEN = 'access';
export const REFRESH_TOKEN = 'refresh';
